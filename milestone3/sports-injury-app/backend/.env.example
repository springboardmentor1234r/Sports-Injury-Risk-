DATABASE_URL=postgresql://sports_user:sports_pass@localhost:5432/sports_injury_db
SECRET_KEY=replace-this-with-a-long-random-string

# Only needed if you run the automated test suite (pytest). Point this at a
# SEPARATE database from DATABASE_URL above -- the tests TRUNCATE every table
# before each test run, so using your dev database here would wipe your data.
TEST_DATABASE_URL=postgresql://sports_user:sports_pass@localhost:5432/sports_injury_test_db

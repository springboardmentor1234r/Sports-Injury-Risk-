# Developer Guide

## Setup
Run `make setup` and `make up`.

## Testing
Run `make test` to execute the test suite.

## Code Style
We follow PEP 8 for Python and ESLint standard for JavaScript/TypeScript.

## Branching
Use feature branches and create pull requests to `main`.

import React from 'react';
import { formatRiskScore, formatTimestamp, getRiskBadgeClass } from '../analysis/analysisFormatters';

const AnalysisHistoryTable = ({ items, onView, onDelete, deletingId }) => (
  <div className="overflow-x-auto rounded-2xl border border-brand-200 bg-white shadow-[0_10px_30px_rgba(234,88,12,0.08)]">
    <table className="min-w-full text-left text-sm"><thead className="bg-gradient-to-r from-brand-500 to-brand-600 text-xs uppercase tracking-wider text-white"><tr><th className="px-4 py-3">Athlete</th><th className="px-4 py-3">Video</th><th className="px-4 py-3">Risk</th><th className="px-4 py-3">Frames</th><th className="px-4 py-3">Analyzed</th><th className="px-4 py-3">Actions</th></tr></thead><tbody className="divide-y divide-brand-100 bg-white">{items.length ? items.map((item) => <tr key={item._id} className="text-slate-600 transition-colors hover:bg-brand-50/70"><td className="px-4 py-3"><p className="font-semibold text-slate-800">{item.athleteId?.fullName || 'Unknown athlete'}</p><p className="text-xs text-slate-500">{item.athleteId?.sport || ''}</p></td><td className="max-w-48 truncate px-4 py-3">{item.video?.originalFileName}</td><td className="px-4 py-3"><span className={`rounded-full border px-2.5 py-1 text-xs font-semibold ${getRiskBadgeClass(item.riskLevel)}`}>{item.riskLevel} · {formatRiskScore(item.riskScore)}</span></td><td className="px-4 py-3">{item.frameCount}</td><td className="px-4 py-3 text-xs text-slate-500">{formatTimestamp(item.createdAt)}</td><td className="px-4 py-3"><div className="flex gap-2"><button onClick={() => onView(item._id)} className="rounded-lg border border-brand-400 bg-white px-3 py-1.5 text-xs font-semibold text-brand-600 transition hover:-translate-y-0.5 hover:bg-brand-50">View</button><button onClick={() => onDelete(item)} disabled={deletingId === item._id} className="rounded-lg border border-red-400 bg-white px-3 py-1.5 text-xs font-semibold text-red-500 transition hover:-translate-y-0.5 hover:bg-red-50 disabled:opacity-60">{deletingId === item._id ? 'Deleting…' : 'Delete'}</button></div></td></tr>) : <tr><td colSpan="6" className="px-4 py-10 text-center text-slate-500">No saved analyses match these filters.</td></tr>}</tbody></table>
  </div>
);

export default AnalysisHistoryTable;
